# 0003 — 레슨 3: TensorFlow 선형 회귀 (코드 읽기)

_기록일: 2026-07-16_

## 계기 (학습자 결정)
학습자가 "코드로" 진행 선택. 동시에 확인: **Python 경험 없음 + TensorFlow 실행 환경 없음.**
요청: "HTML에 예상 결과까지 같이 적어달라."

## 교육 결정
- lab을 **read-only 모드**로 설계: 실행 요구 0, 코드를 5조각(데이터/W·b/가설/cost/최소화)+실행루프로 끊어
  각 조각을 레슨 2 개념과 1:1 매핑. **예상 출력을 HTML에 내장** (검은 터미널 박스, 수렴 트레이스).
- 출력 숫자는 자체 정합성 있게 구성(W→1.004, b→-0.009, cost→~1e-5)하되 "예시값·수렴방향 동일" 명시.
- Python 무경험 대응: 코드 전에 6줄짜리 초경량 primer(=, 리스트, 호출, for/range, if/%, print).
- 원본이 TF 1.x라 버전 주의 콜아웃 추가. 재작성(TF2) 안 함 — 실행 안 하니 원본 충실이 덜 혼란.
- 재사용 자산 신설: `reference/tensorflow-linreg.html` (5+1 골격 + 개념↔코드 표 + placeholder 변형).
  다음 랩들이 이 치트시트 위에 얹힘.

## 순서 변경
- 기존 로드맵의 L3(경사하강법)은 **L4로 밀림.** 실제 순서: L2(lec02)→L3(lab02 코드)→L4(lec03 경사하강법).
  학습자가 개념→코드 인터리빙을 선호 → 앞으로도 lec 뒤 대응 lab을 바로 붙이는 리듬 고려.

## 다음 (L4 후보)
- lec 03 경사하강법: "cost 최소화 마법 버튼"의 정체. 안대 쓰고 언덕 내려가기, 기울기=방향, learning_rate=보폭.
  레슨 3에서 이미 learning_rate·minimize를 "다음에 설명"으로 걸어둠 → 자연스러운 연결.
- 이후 lab 03(cost 최소화 시각화)도 코드-읽기 모드로 대응 가능.
