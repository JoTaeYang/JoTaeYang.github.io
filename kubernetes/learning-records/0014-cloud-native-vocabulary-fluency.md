# Cloud-native vocabulary batch passed; all low-weight KCNA domains covered

Lesson 0014 quizzes 3/3. Covered the three low-weight domains in one pass at vocabulary level (no over-investment): Cloud Native Architecture (16%), Observability (8%), Application Delivery (8%).

**Evidence (2026-07-07):** 3/3 — Q1 CNCF maturity order (Sandbox→Incubating→Graduated), Q2 traces = the request-path pillar (metrics=number / logs=sentence / traces=map), Q3 (spaced from 0007) GitOps = the control loop with desired state living in Git instead of etcd. Cross-links baked in: 12-factor config→0005, disposable→0002, state-outside→0011; OCI→0012; observability logs→0004/0006 kubectl logs; Helm chart/values/release framed as apt-for-Kubernetes over the hand-written YAML the learner has been producing.

**Implications:** KCNA gap-check is now all ✅ at the required depth — every domain has lesson/lab or vocabulary evidence. Only remaining step is the full-domain mock diagnostic (Lesson 0015) mixing all five domains to surface any residual gap, then set an exam date. Also added a lessons index (index.html at mastery root) linking all 14 lessons + gap-check + roadmap, grouped by domain. Learner pace remains intensive; questions keep probing below the KCNA line (CKA-depth banked early).
