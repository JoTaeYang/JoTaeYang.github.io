# Interface trio + Pod-vs-Service network deep dive both passed; network intuition solid

Lessons 0012 (CRI/CNI/CSI) and 0013 (Pod network vs Service network) quizzes both 3/3.

**Evidence (2026-07-07):** 0012 3/3 and 0013 3/3. 0013 was expanded from the learner's unprompted question (see LR-0012) into a full lesson and then, on request, grown three times: (1) a tighter "30-second summary" box at the top plus the *why* of the split (a never-changing signboard in front of ever-churning Pods), (2) a "CIDR 읽는 법" appendix (32-bit / `/n` fixed-vs-free bits, `/8`→`/32` range table, why Pod CIDR and Service CIDR must not overlap), and (3) a "요청의 여정" section tracing one `curl web` end-to-end: DNS name→ClusterIP, then kube-proxy DNAT ClusterIP→live Pod IP, then CNI-laid road to the real Pod, then reverse un-DNAT. External path folds into the same DNAT chain via NodePort (0010).

**Implications:** Container Orchestration (~22%) is now ✅ with lab evidence; the learner's questions are consistently probing *below* the KCNA line (virtual IPs, DNAT, CIDR), which is CKA-troubleshooting depth banked early. Main track resumes at the last gap: cloud-native vocabulary batch (Lesson 0014) covering the three low-weight domains at once — Cloud Native Architecture (16%: CNCF maturity, 12-factor, micro vs mono, OCI), Observability (8%: metrics/logs/traces three pillars), Application Delivery (8%: GitOps, Helm). Strategy holds: vocabulary level only, no over-investment; GitOps reconciliation cross-links to the control loop (0007 controller-manager), 12-factor config to 0005. After 0014 → full-domain KCNA mock diagnostic → exam scheduling.
