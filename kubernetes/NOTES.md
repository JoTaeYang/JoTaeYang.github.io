# Working Notes

## Learner preferences (carried over from ../teach/ workspace)

- Lessons and user-facing content in Korean; code/commands/identifiers stay in English.
- Short lessons, one tangible win each, no concept jumps.
- Shared assets: `assets/styles.css` + `assets/quiz.js` (copied from ../teach/, keep visual consistency).

## Workspace boundary

- This workspace = generic beginner→expert + 5-cert (Kubestronaut) track, practice on disposable clusters (kind/killercoda).
- `../teach/` = separate track about the real mgmt/tkai3 production cluster architecture. Do not mix content; cross-link only when genuinely useful.

## Pace

- Learner chose an intensive pace ("미친듯이 공부할 거임") — no weekly-hours constraint. Produce lessons at high frequency; keep each one short with a single win.
- Diagnostic done (see LR-0002): start at Level 1 (resource model). Answers were memory-based → bake spaced retrieval of earlier topics into every lesson.

## Open questions for the learner

- Target date/budget for first exam (KCNA) — ask once Level 1 lessons are underway.
- Confirm cert order KCNA → KCSA → CKAD → CKA → CKS (proposed; CKS requires active CKA).
